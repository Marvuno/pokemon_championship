"""
The title screen's artwork used to live here as block letters made of box
characters, printed to a console. The window draws its own title screen, so
there is nothing left to draw here -- `cover_art` stays as an empty string
because start_interface still prints it on the way in.
"""

cover_art = ""
